# Core Dependencies
python>=3.10

# Excel Processing
openpyxl==3.1.2              # Read/write Excel files (XLSX)
xlrd==2.0.1                  # Read old Excel files (XLS) - optional
pandas==2.1.4                # Data manipulation (used sparingly)
numpy==1.26.2                # Numerical operations

# LLM & Agent Framework
boto3==1.34.18               # AWS SDK for Bedrock
botocore==1.34.18            # AWS core functionality
langchain==0.1.0             # LangChain framework
langchain-aws==0.1.0         # LangChain AWS integration
langgraph==0.0.20            # LangGraph for agent workflows
langchain-core==0.1.10       # LangChain core components

# Caching
diskcache==5.6.3             # Persistent disk-based cache
cachetools==5.3.2            # In-memory caching utilities

# API Framework
fastapi==0.109.0             # Modern web framework
uvicorn[standard]==0.26.0    # ASGI server
python-multipart==0.0.6      # File upload support
pydantic==2.5.3              # Data validation
pydantic-settings==2.1.0     # Settings management

# Search & Text Processing
fuzzywuzzy==0.18.0           # Fuzzy string matching
python-Levenshtein==0.23.0   # Fast string similarity (optional but recommended)
regex==2023.12.25            # Advanced regex support

# Utilities
python-dotenv==1.0.0         # Environment variable management
tenacity==8.2.3              # Retry logic
loguru==0.7.2                # Advanced logging
python-dateutil==2.8.2       # Date parsing utilities
filelock==3.13.1             # File locking for concurrent access

# Data Validation & Typing
typing-extensions==4.9.0     # Backported typing features

# Development & Testing (optional - install with pip install -e ".[dev]")
# pytest==7.4.4
# pytest-asyncio==0.23.3
# pytest-cov==4.1.0
# black==24.1.1
# ruff==0.1.14
# mypy==1.8.0
# ipython==8.20.0
# jupyter==1.0.0

# Performance Monitoring (optional)
# memory-profiler==0.61.0
# psutil==5.9.7

# Note: ripgrep must be installed separately via system package manager
# Ubuntu/Debian: sudo apt-get install ripgrep
# macOS: brew install ripgrep
# Windows: choco install ripgrep
# Or download from: https://github.com/BurntSushi/ripgrep/releases